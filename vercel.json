{
  "version": 2,
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    },
    {
      "src": "api/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/generate_project", "dest": "/api/generate_project.js" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
